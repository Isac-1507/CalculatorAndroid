import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Isac_1507.Calculator',
  appName: 'Calculator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
